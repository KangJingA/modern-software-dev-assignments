# Project Instructions

## Python Environment

Always activate the conda environment before running Python commands:

```bash
source /Users/nicholas.ang/miniconda3/bin/activate && conda activate cs146s
```

Apply this to every terminal command that invokes Python, pytest, poetry, or any project tooling.
